# Plugin Display Labels

- User decision: Codex quota window labels may be configured independently; Automatic retains the duration-derived label, and a configured value applies to both compact and large renderers.
- User decision: Water Heater remaining and EMV labels may be configured independently, defaulting to `HW` and `MV`.
